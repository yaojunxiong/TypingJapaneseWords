// Minna Course Admin v17.1
// JSON course maintenance panel. First version: load, validate, edit, download JSON safely.
(function(){
  const VERSION='17.1';
  const $=id=>document.getElementById(id);
  let currentNo=1,currentJson=null;
  function pad(n){return String(n).padStart(2,'0')}
  function esc(s){return String(s==null?'':s).replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]))}
  function pick(v){return v&&(v.zh||v.en||v.ja)||''}
  function path(no){return `./data/minna/lessons/lesson-${pad(no)}.json?v=${VERSION}`}
  function shell(){return `<section class="panel" id="courseAdminV17"><h2>课程 JSON 维护 v17.1</h2><p class="small">读取、编辑、校验并下载课程 JSON。第一版先不直接写 GitHub，避免误覆盖线上课程。</p><div class="filters"><select id="courseNoSelect">${Array.from({length:50},(_,i)=>`<option value="${i+1}">第${i+1}课 / Lesson ${i+1}</option>`).join('')}</select><button class="dark" id="loadCourseJsonBtn">读取 JSON</button><button class="light" id="validateJsonBtn">校验 JSON</button><button class="primary" id="downloadJsonBtn">下载修改后的 JSON</button></div><div id="courseJsonSummary" class="small">请选择课程并读取 JSON。</div><textarea id="courseJsonEditor" class="jsonEditor" spellcheck="false" placeholder="课程 JSON 会显示在这里"></textarea><p id="courseJsonStatus" class="small"></p></section>`}
  function injectStyle(){if($('courseAdminStyleV17'))return;const s=document.createElement('style');s.id='courseAdminStyleV17';s.textContent='.jsonEditor{width:100%;min-height:520px;border:1px solid #e2e8f0;border-radius:18px;padding:14px;font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,monospace;font-size:13px;line-height:1.5;background:#0f172a;color:#e2e8f0;margin-top:12px}.jsonSummaryGrid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0}.jsonSummaryGrid div{background:#f8fafc;border:1px solid #e2e8f0;border-radius:16px;padding:10px;text-align:center}.jsonSummaryGrid b{display:block;font-size:24px}.jsonOk{color:#166534;font-weight:900}.jsonBad{color:#991b1b;font-weight:900}@media(max-width:760px){.jsonSummaryGrid{grid-template-columns:1fr 1fr}.jsonEditor{min-height:420px}}';document.head.appendChild(s)}
  function install(){if($('courseAdminV17'))return;const main=document.querySelector('main');if(!main)return;const temp=document.createElement('div');temp.innerHTML=shell();const ref=[...main.querySelectorAll('.panel')].find(x=>(x.textContent||'').includes('最近访客记录'));main.insertBefore(temp.firstElementChild,ref||main.firstChild);bind()}
  function bind(){
    $('loadCourseJsonBtn').onclick=()=>loadJson(Number($('courseNoSelect').value));
    $('validateJsonBtn').onclick=validateEditor;
    $('downloadJsonBtn').onclick=downloadEditor;
  }
  async function loadJson(no){currentNo=no;$('courseJsonStatus').textContent='读取中：'+path(no);try{const res=await fetch(path(no),{cache:'no-store'});if(!res.ok)throw new Error('JSON 不存在或读取失败：HTTP '+res.status);currentJson=await res.json();$('courseJsonEditor').value=JSON.stringify(currentJson,null,2);renderSummary(currentJson);$('courseJsonStatus').innerHTML='<span class="jsonOk">读取成功</span>：'+esc(path(no))}catch(e){currentJson=null;$('courseJsonEditor').value='';$('courseJsonSummary').innerHTML='<span class="jsonBad">读取失败：</span>'+esc(e.message)+'<br><span class="small">可以先复制现有模板，新建 lesson-'+pad(no)+'.json。</span>';$('courseJsonStatus').innerHTML='<span class="jsonBad">'+esc(e.message)+'</span>'}}
  function validate(obj){const errors=[];if(!obj||typeof obj!=='object')errors.push('根节点必须是 object');if(!obj.schema)errors.push('缺少 schema');if(!obj.lessonNo)errors.push('缺少 lessonNo');if(!obj.lessonId)errors.push('缺少 lessonId');if(!obj.title||!obj.title.zh)errors.push('缺少 title.zh');if(!Array.isArray(obj.sections))errors.push('sections 必须是数组');else{obj.sections.forEach((s,i)=>{if(!s.type)errors.push(`sections[${i}] 缺少 type`);if(!s.id)errors.push(`sections[${i}] 缺少 id`);if(!Array.isArray(s.items))errors.push(`sections[${i}].items 必须是数组`)})}return errors}
  function renderSummary(obj){const sections=obj.sections||[];const vocab=sections.filter(s=>s.type==='vocab').reduce((n,s)=>n+(s.items||[]).length,0);const grammar=sections.filter(s=>s.type==='grammar').reduce((n,s)=>n+(s.items||[]).length,0);const examples=sections.filter(s=>s.type==='examples').reduce((n,s)=>n+(s.items||[]).length,0);const quiz=sections.filter(s=>s.type==='quiz').reduce((n,s)=>n+(s.items||[]).length,0);$('courseJsonSummary').innerHTML=`<h3>${esc(pick(obj.title))}｜${esc(pick(obj.subtitle))}</h3><p>${esc(pick(obj.focus))}</p><div class="jsonSummaryGrid"><div><b>${vocab}</b><span>词汇</span></div><div><b>${grammar}</b><span>语法</span></div><div><b>${examples}</b><span>例句</span></div><div><b>${quiz}</b><span>测试题</span></div></div><p class="small">lessonId: <code>${esc(obj.lessonId)}</code> · schema: <code>${esc(obj.schema)}</code></p>`}
  function validateEditor(){try{const obj=JSON.parse($('courseJsonEditor').value);const errors=validate(obj);if(errors.length){$('courseJsonStatus').innerHTML='<span class="jsonBad">校验失败：</span><br>'+errors.map(esc).join('<br>');return false}currentJson=obj;renderSummary(obj);$('courseJsonStatus').innerHTML='<span class="jsonOk">JSON 合法，可以下载保存。</span>';return true}catch(e){$('courseJsonStatus').innerHTML='<span class="jsonBad">JSON 语法错误：</span>'+esc(e.message);return false}}
  function downloadEditor(){if(!validateEditor())return;const blob=new Blob([$('courseJsonEditor').value],{type:'application/json;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='lesson-'+pad(currentJson.lessonNo||currentNo)+'.json';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
  function start(){injectStyle();install()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();
